# OnlineShopBackendSystem
Online shop backend built with Clean Architecture (.NET, EF Core, PostgreSQL). Supports products, cart, orders. Handles concurrency (prevent oversell), idempotent order processing, background jobs, and event-driven workflows. Includes JWT auth, policy-based authorization, caching, logging, and scalable API design.
